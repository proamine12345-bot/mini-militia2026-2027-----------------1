class Particles{constructor(){this.items=[]}burst(x,y,color,count=10,power=220){for(let i=0;i<count;i++){let a=Math.random()*Math.PI*2,s=Math.random()*power;this.items.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:V.rand(.25,.7),size:V.rand(2,6),color})}}update(dt){for(let p of this.items){p.life-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=500*dt}this.items=this.items.filter(p=>p.life>0)}draw(c,cam){for(let p of this.items){c.globalAlpha=Math.max(0,p.life*2);c.fillStyle=p.color;c.fillRect(p.x-cam.x,p.y-cam.y,p.size,p.size)}c.globalAlpha=1}}